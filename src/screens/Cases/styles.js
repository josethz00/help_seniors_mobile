import { StyleSheet, Dimensions } from 'react-native';

const styles = StyleSheet.create({
    sectionName: {
        textAlign: 'center',
        marginTop: 25,
        marginBottom: 5,
        color: '#444',
        fontWeight: 'bold',
        fontSize: 18
    },
    list: {
        paddingBottom: 100,
    }
});

export default styles;